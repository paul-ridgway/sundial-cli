{
  "name": "sundial-cli",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node src/index.ts",
    "dev": "ts-node-dev --respawn src/index.ts",
    "build": "tsc -p .",
    "check": "npm run compile && npm run lint:all",
    "compile:watch": "tsc --watch --noEmit --skipLibCheck",
    "compile": "tsc --noEmit --skipLibCheck"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/paul-ridgway/sundial-cli.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/paul-ridgway/sundial-cli/issues"
  },
  "homepage": "https://github.com/paul-ridgway/sundial-cli#readme",
  "dependencies": {
    "sunsynk-node-api-client": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.15.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.5"
  }
}
